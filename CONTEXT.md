# Time Bomb Project Context

이 문서는 공개 저장소에서 사람과 AI 에이전트가 공통으로 알아야 하는 최소 제품 맥락만 정의한다. 상세 비즈니스 정책, 시스템 구조, 운영 정보와 보안 관련 설계는 담지 않는다.

## 제품 정체성

- 현재 제품명은 `Time Bomb`이다.
- 제품명은 `Whiplash`에서 `눈 떠!`, 다시 `Time Bomb` 순서로 변경되었다.
- `Whiplash-Server` 저장소명, `akuma.whiplash` Java package와 기존 앱·인프라 식별자에 남아 있는 `Whiplash` 또는 `nuntteo`는 이전 이름에서 이어진 기술 식별자다.
- 기술 식별자는 별도 migration 계획 없이 제품명 변경만을 이유로 수정하지 않는다.
- 새 제품 문서와 사용자 대상 표현은 `Time Bomb`을 사용한다. 과거 작업 이력과 외부 시스템의 기존 고유명은 원래 이름을 유지할 수 있다.

## 제품 정의

Time Bomb은 사용자가 설정한 시간에 목표 행동을 수행하도록 돕는 알람 서비스다.

## 공통 용어

- `Member`: 서비스 계정의 주체다.
- `Device`: 회원이 서비스를 사용하는 앱 설치 단위다.
- `Alarm`: 사용자가 설정한 반복 알람 규칙이다.
- `Alarm Occurrence`: Alarm에서 특정 시점에 발생하는 한 번의 실행 단위다.
- `Place`: 알람과 관련된 장소 정보다.

Alarm 설정의 생명주기와 Alarm Occurrence 한 회차의 생명주기를 혼용하지 않는다.

## 문서 사용 원칙

- 공통 개발 규칙은 `AGENTS.md`를 따른다.
- 문서별 역할과 읽기 순서는 `docs/README.md`를 따른다.
- 상세 비즈니스 정책, API 계약, 아키텍처, 운영 정보와 작업 이력은 Git에서 제외된 로컬 `docs/` 문서에서 관리한다.
- 이 문서에는 인증 방식, 데이터 구조, 내부 상태값, 임계값, 방어 로직, 인프라 구성처럼 리버스 엔지니어링에 도움이 되는 세부 정보를 추가하지 않는다.
- 공개하기 어려운 개인 메모는 `CONTEXT.local.md`에 기록한다.
